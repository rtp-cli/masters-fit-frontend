import { ExcludedExercisesScreen } from "@/components/excluded-exercises";

// Routing only — Settings → Training profile → Excluded exercises (1g).
export default function ExcludedExercises() {
  return <ExcludedExercisesScreen />;
}
